# `interoptopus_backends`

Shared utilities for [Interoptopus](https://crates.io/crates/interoptopus) backends.

Provides common helpers used across backends: template rendering (via Tera),
output buffering, and casing utilities.

This crate is an implementation detail; prefer depending on a specific backend crate directly.
