interoptopus::plugin!(Primitives {
    fn primitive_void();
    fn primitive_u32(x: u32) -> u32;
});

interoptopus::plugin!(Primitives2 {
    fn primitive_void();
    fn primitive_u32(x: u32) -> u32;
});
